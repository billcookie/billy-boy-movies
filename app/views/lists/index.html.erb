<h1>Movies!!!!</h1>
<div class="container">

  <%= link_to "Create List", new_list_path, class: "btn btn-flat" %>

  <% @lists.each do |list| %>
    <ul>
      <%= list.photo.key %>
      <%= link_to list.name, list_path(list) %>
    </ul>
  <% end %>
</div>
